# Where this meets Rust

<span class="todo">placeholder</span>

## TODO

Consolidates the **Coming up in Rust** callouts scattered through the book, as a single page to read *after* the fearless-concurrency chapter rather than before.

- [ ] `rayon`: `par_iter().fold(identity, op).reduce(identity, op)`
- [ ] Why the identity is an explicit argument there and a hidden arity here
- [ ] Ownership and `Send`/`Sync` vs. persistent data structures — two routes to the same guarantee
- [ ] Work-stealing in both runtimes
